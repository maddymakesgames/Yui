// This event executes when a new guild (server) is left.

module.exports = (client, guild) => {
  //Currently commented out to support people reinviting the bot and keeping their settings. 
  //client.settings.delete(guild.id);
};
